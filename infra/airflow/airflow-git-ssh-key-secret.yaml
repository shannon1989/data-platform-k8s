apiVersion: v1
data:
  gitSshKey: Generated from "kubectl create secret generic airflow-git-ssh-key-secret --from-file=gitSshKey=./airflow_gitsync_id_rsa --namespace airflow --dry-run=client -o yaml > airflow-git-ssh-key-secret.yaml"
kind: Secret
metadata:
  name: airflow-git-ssh-key-secret
  namespace: airflow
