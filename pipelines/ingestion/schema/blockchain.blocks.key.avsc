{
  "type": "record",
  "name": "BlockKey",
  "namespace": "blockchain.blocks",
  "fields": [
    {
      "name": "chain_id",
      "type": "string",
      "doc": "Logical chain identifier, e.g. eth-mainnet, sol-mainnet, sui-mainnet"
    },
    {
      "name": "block_height",
      "type": "long",
      "doc": "Block number / slot / checkpoint"
    }
  ]
}

