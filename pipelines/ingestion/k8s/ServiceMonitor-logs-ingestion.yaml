apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: logs-ingestion
  namespace: monitoring
  labels:
    release: prometheus
spec:
  namespaceSelector:
    matchNames:
      - airflow
  selector:
    matchLabels:
      app: logs-ingestion
  endpoints:
    - port: metrics
      path: /metrics
      interval: 8s
