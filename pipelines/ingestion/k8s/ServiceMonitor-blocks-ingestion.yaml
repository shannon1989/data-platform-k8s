apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: blocks-ingestion
  namespace: prometheus
  labels:
    release: prometheus
spec:
  namespaceSelector:
    matchNames:
      - airflow
  selector:
    matchLabels:
      app: blocks-ingestion
  endpoints:
    - port: metrics
      path: /metrics
      interval: 8s
