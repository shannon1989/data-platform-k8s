apiVersion: v1
kind: Service
metadata:
  name: blocks-ingestion-metrics
  namespace: airflow
  labels:
    app: blocks-ingestion
spec:
  selector:
    app: blocks-ingestion
  ports:
    - name: metrics
      port: 8000
      targetPort: 8000
